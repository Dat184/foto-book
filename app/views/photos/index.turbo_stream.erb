<%= turbo_stream.append :photos do %>
  <%= render @photos %>
<% end %>

<% if @pagy.next.present? %>
  <%= turbo_stream.replace :next_page do %>
    <%= turbo_frame_tag :next_page, src: photos_path(page: @pagy.next, format: :turbo_stream), loading: :lazy %>
  <% end %>
<% end %>