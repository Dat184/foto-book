<%= turbo_stream.append :albums do %>
  <%= render @albums %>
<% end %>

<% if @pagy.next.present? %>
  <%= turbo_stream.replace :next_page do %>
    <%= turbo_frame_tag :next_page, src: albums_path(page: @pagy.next, format: :turbo_stream), loading: :lazy %>
  <% end %>
<% end %>